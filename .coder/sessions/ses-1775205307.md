---
id: ses-1775205307
saved: 2026-04-03T15:35:07+07:00
status: active
---

# Session: Suppress the known LangGraph/langchain_core Python 3.14 deprecation warning in pytest output without hiding unrelated warnings, and verify the full Minder test suite stays green.

## Current Task
Suppress the known LangGraph/langchain_core Python 3.14 deprecation warning in pytest output without hiding unrelated warnings, and verify the full Minder test suite stays green.

## Next Steps
1. Revisit this filter once upstream langchain_core or langgraph removes the Python 3.14 Pydantic v1 warning path.
2. Prefer removing the suppression rather than widening it if dependencies become 3.14-clean.

## Open Files
- pytest.ini
- tests/unit/test_runtime_detection.py

## Recent Decisions
- The warning originates in external dependency code from langchain_core when LangGraph is imported on Python 3.14, so the least risky fix is a targeted pytest filter instead of broader warning suppression.
- The filter is scoped to the exact warning message and module path so unrelated warnings still surface in CI and local runs.
- Validated with UV_CACHE_DIR=.uv-cache uv run pytest tests/unit/test_runtime_detection.py and UV_CACHE_DIR=.uv-cache uv run pytest; both passed with no warning output.

