---
id: ses-1775206882
saved: 2026-04-03T16:01:22+07:00
status: active
---

# Session: Update Minder roadmap and task tracking to switch the target infra stack from SQLite/Milvus Lite assumptions to MongoDB + Redis + Milvus Standalone managed by Docker Compose, with an explicit future upgrade path toward clustered deployment.

## Current Task
Update Minder roadmap and task tracking to switch the target infra stack from SQLite/Milvus Lite assumptions to MongoDB + Redis + Milvus Standalone managed by Docker Compose, with an explicit future upgrade path toward clustered deployment.

## Next Steps
1. Start Wave 5 by redesigning config and repository interfaces around MongoDB as the primary operational store while keeping the application layer stable.
2. Add Redis config/runtime seams next so cache/session responsibilities are explicit before touching the deployment stack.
3. Rework docker-compose.dev.yml after that to run app + MongoDB + Redis + Milvus Standalone together, then re-baseline P1-VERIFY against the new infra shape.

## Open Files
- docs/TASK_BREAKDOWN.md
- docs/PROJECT_PROGRESS.md

## Recent Decisions
- Phase 1 is no longer considered closable on the old SQLite/Milvus Lite target; the infra baseline has been reset to MongoDB + Redis + Milvus Standalone.
- P1-T04 was reopened as MongoDB work, with a new P1-T04A migration task for repository/index/bootstrap/backfill concerns.
- A new P1-T11A task now tracks Redis as a first-class runtime service instead of leaving cache/session behavior implicit.
- P1-T19 and P1-VERIFY were re-scoped so Docker Compose and acceptance testing must prove the full multi-service stack, not just the app container.
- Phase 4 was also corrected so future scale work means MongoDB topology, Redis HA, and Milvus cluster evolution rather than a late switch to PostgreSQL.

