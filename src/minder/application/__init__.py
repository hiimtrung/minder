"""
Application Layer — Clean Architecture use-case layer.

This package contains application services (use cases) that orchestrate
domain logic. Use-case services depend on domain interfaces and are
called by presentation adapters (MCP tools, HTTP routes).
"""
