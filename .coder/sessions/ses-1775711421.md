---
id: ses-1775711421
saved: 2026-04-09T12:10:21+07:00
status: active
---

# Session: Made onboarding snippets and connection-test templates derive their base URL from the current HTTP request origin instead of hardcoded localhost, while keeping a localhost fallback for pure use-case calls.

## Current Task
Made onboarding snippets and connection-test templates derive their base URL from the current HTTP request origin instead of hardcoded localhost, while keeping a localhost fallback for pure use-case calls.

## Next Steps
1. Rebuild and verify the dashboard in a deployed environment so snippets now show the real domain instead of localhost.
2. If a reverse proxy is used, confirm the app receives the correct external origin or add trusted forwarded-host handling later.

## Open Files
- src/minder/application/admin/use_cases.py
- src/minder/presentation/http/admin/api.py
- tests/integration/test_phase4_http_admin.py
- tests/e2e/test_phase4_gateway_auth.py

## Recent Decisions
- Keep URL derivation at the presentation layer: HTTP routes compute the public origin from request.base_url and pass it into admin use cases.
- Do not hardcode localhost into onboarding snippets when serving over HTTP; only use localhost as a fallback for direct unit/use-case calls with no request context.

