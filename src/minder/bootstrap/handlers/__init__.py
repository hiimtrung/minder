"""
Bootstrap Handler Modules — extracted from the transport.py God Function.

Each module contains handler functions for a specific domain area,
keeping the main build_transport() function as a pure wiring/composition root.
"""
