---
id: ses-1775705243
saved: 2026-04-09T10:27:23+07:00
status: active
---

# Session: Moved the Astro admin console to src/dashboard as the primary dashboard surface, switched serving from /console to /dashboard, added production Docker build/runtime wiring for the static bundle, updated browser/admin tests to Astro-shell semantics, and wrote production deployment docs.

## Current Task
Moved the Astro admin console to src/dashboard as the primary dashboard surface, switched serving from /console to /dashboard, added production Docker build/runtime wiring for the static bundle, updated browser/admin tests to Astro-shell semantics, and wrote production deployment docs.

## Next Steps
1. Run astro dependency install and build verification inside Docker or local frontend workspace.
2. Update remaining docs/tests that still describe the old server-rendered dashboard as current UI.
3. Optionally add a production health endpoint or deploy smoke check that verifies /dashboard assets are present at startup.

## Open Files
- src/minder/presentation/http/admin/routes.py
- src/dashboard/src/pages/clients/index.astro
- docker/Dockerfile
- docker/docker-compose.prod.yml
- docs/guides/production-deployment.md

## Recent Decisions
- Serve Astro as the default admin console on /dashboard and keep /console as a compatibility redirect only.
- Bake the Astro static build into the Python Docker image at /app/dashboard-dist so production remains one container and one port.
- Use a single static clients page plus Python fallback to clients/index.html for /dashboard/clients/{id} to avoid dynamic build-time routing requirements.
- Keep old POST-based browser endpoints as compatibility seams while the new Astro console uses JSON admin APIs.

