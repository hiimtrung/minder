---
id: ses-1775204689
saved: 2026-04-03T15:24:49+07:00
status: active
---

# Session: Enable real compose runtimes for Minder dev stack by wiring optional LangGraph, llama-cpp-python, and LiteLLM into Docker compose, hardening runtime fallbacks so boot/scripts/tests stay stable when model init or network fallback fails.

## Current Task
Enable real compose runtimes for Minder dev stack by wiring optional LangGraph, llama-cpp-python, and LiteLLM into Docker compose, hardening runtime fallbacks so boot/scripts/tests stay stable when model init or network fallback fails.

## Next Steps
1. Rebuild docker compose and inspect the printed runtime summary to confirm orchestration_runtime_effective, llm_runtime_effective, embedding_runtime_effective, and openai_fallback_runtime_effective.
2. If llama_cpp still falls back to mock, validate the GGUF filenames under ~/.minder/models and check whether the selected models can initialize on the container CPU/memory profile.
3. Consider adding an explicit healthcheck or startup tool endpoint that returns runtime_summary for manual sign-off before Phase 3.

## Open Files
- docker/docker-compose.dev.yml
- src/minder/server.py
- src/minder/graph/graph.py
- src/minder/llm/qwen.py
- src/minder/embedding/qwen.py
- src/minder/llm/openai.py
- tests/unit/test_runtime_detection.py
- tests/integration/test_phase1_wave3_assets.py

## Recent Decisions
- Compose now requests LangGraph explicitly and points LLM and embedding paths at mounted GGUF files under /root/.minder/models.
- MinderGraph now constructs QwenLocalLLM and OpenAIFallbackLLM with runtime="auto" so installed runtimes are actually used instead of staying on mock.
- QwenLocalLLM and QwenEmbeddingProvider now catch llama_cpp initialization errors and fall back cleanly instead of crashing boot or seed scripts.
- OpenAIFallbackLLM now catches LiteLLM/provider errors and returns fallback text, which keeps tests and offline environments stable while still reporting litellm as the attempted runtime.
- Server startup prints a runtime summary so manual verification can confirm whether the stack is using langgraph, llama_cpp, litellm, or mock/internal fallback.

