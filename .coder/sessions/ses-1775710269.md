---
id: ses-1775710269
saved: 2026-04-09T11:51:09+07:00
status: active
---

# Session: Fixed final Astro script integration issue by replacing ?url TypeScript asset loading with Astro-bundled <script src="../scripts/*.ts"></script> usage so browser module scripts resolve to built JavaScript instead of raw .ts assets with the wrong MIME type.

## Current Task
Fixed final Astro script integration issue by replacing ?url TypeScript asset loading with Astro-bundled <script src="../scripts/*.ts"></script> usage so browser module scripts resolve to built JavaScript instead of raw .ts assets with the wrong MIME type.

## Next Steps
1. Rebuild src/dashboard and the Docker image, then verify /dashboard/setup and /dashboard/login no longer request /lib/api/admin or .ts assets directly.

## Open Files
- src/dashboard/src/pages/setup.astro
- src/dashboard/src/pages/login.astro
- src/dashboard/src/pages/clients/index.astro
- tests/unit/test_phase4_3_dashboard_scaffold.py

## Recent Decisions
- Do not use ?url for local TypeScript page scripts in Astro here; it produces raw .ts asset URLs that the Python static server serves with the wrong MIME type.
- Use Astro-bundled <script src="../scripts/*.ts"></script> so Vite emits proper JS assets under /dashboard/_astro.

