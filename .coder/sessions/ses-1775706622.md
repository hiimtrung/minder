---
id: ses-1775706622
saved: 2026-04-09T10:50:22+07:00
status: active
---

# Session: Fixed Docker dashboard-builder install flow for Astro by splitting esbuild installation into a separate step: npm install --ignore-scripts, then node node_modules/esbuild/install.js, then npm run build. This avoids the ETXTBSY failure thrown when esbuild validates its binary during the same npm install layer on Node Alpine.

## Current Task
Fixed Docker dashboard-builder install flow for Astro by splitting esbuild installation into a separate step: npm install --ignore-scripts, then node node_modules/esbuild/install.js, then npm run build. This avoids the ETXTBSY failure thrown when esbuild validates its binary during the same npm install layer on Node Alpine.

## Recent Decisions
- /docker/Dockerfile
- /tests/unit/test_phase4_3_production_assets.py

