---
id: ses-1775707261
saved: 2026-04-09T11:01:01+07:00
status: active
---

# Session: Fixed the dashboard-to-Minder integration flow so the Astro console now follows real setup and session state instead of serving static pages blindly. Added server-side flow guards on /dashboard routes and frontend 401 redirect handling, then updated the flow tests to match the new routing semantics.

## Current Task
Fixed the dashboard-to-Minder integration flow so the Astro console now follows real setup and session state instead of serving static pages blindly. Added server-side flow guards on /dashboard routes and frontend 401 redirect handling, then updated the flow tests to match the new routing semantics.

## Next Steps
1. Rebuild the Docker image and verify the Astro bundle serves correctly from /dashboard with the latest base path fix.
2. Update the operator guides to state explicitly that /dashboard is an entrypoint that redirects to /dashboard/setup, /dashboard/login, or /dashboard/clients depending on state.

## Open Files
- src/minder/presentation/http/admin/routes.py
- src/dashboard/src/lib/api/admin.ts
- tests/integration/test_phase4_3_console_gate.py

## Recent Decisions
- Treat /dashboard as a stateful entrypoint: fresh install redirects to /dashboard/setup, unauthenticated admins redirect to /dashboard/login, authenticated admins redirect to /dashboard/clients.
- Keep static Astro asset requests exempt from auth gating so /dashboard/_astro assets and other bundle files still load.
- Client-side admin API calls should redirect 401 responses back to login or setup instead of leaving the user stranded on a partially rendered shell.

